<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bitcamp.op.member.dao.Dao">
<!-- 인터페이스의  full name을 namespace로 설정해야한다!!! -->


	<!-- sql tag 식별은 인터페이스의 메소드 이름과 일치 시킨다  -->
	<!-- 로그인을 위한 select -->
	<select	id = "selectByIdPw"	
			resultType = "com.bitcamp.op.member.domain.Member"
	>
		select * from member1 where memberid=#{param1} and password=#{param2}
	</select>
	
	<select	id="selectById"
			resultType="int"
	>
		select count(*) from member1 where memberid=#{mid}
	</select>
	
	<insert	id="insertMember"
			parameterType="com.bitcamp.op.member.domain.Member"
			useGeneratedKeys="true"
			keyProperty="idx"
	>
		insert into member1 (memberid, password, membername, memberphoto)
		value (#{memberid}, #{password}, #{membername}, #{memberphoto})
	
	</insert>
	
	<update id="updateMember"
			parameterType="com.bitcamp.op.member.domain.Member"
	>
	
		update member1 
		set membername = #{membername}, password = #{password}
		where idx = ${idx}
	
	</update>
	
	<delete	id="deleteMember">
		delete from member1 where idx = #{idx}
	</delete>
	
	<!-- 전체 회원 리스트 -->
	<select id="selectAll"
			resultType="com.bitcamp.op.member.domain.Member"
	>
		select * from member1 order by memberid
	</select>
	
	<!-- 페이징 처리가 가능한 회원 리스트 -->
	<select id="selectList"
			resultType="com.bitcamp.op.member.domain.Member"
	>
		select * from member1 order by memberid limit #{param1}, #{param2}
	</select>

	<select	id="selectTotalCount"
			resultType="int"
	>
		select count(*) from member1
	</select>









	
</mapper>